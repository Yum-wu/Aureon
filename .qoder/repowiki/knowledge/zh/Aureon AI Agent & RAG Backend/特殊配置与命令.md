- **环境初始化**：运行 `setup_venv.sh` (Linux/Mac) 或 `setup_venv.bat` (Windows) 创建虚拟环境并安装依赖。
- **配置要求**：需复制 `.env.example` 为 `.env` 并配置 `LLM_API_KEY` 及向量数据库路径。
- **测试执行**：使用 `pytest` 运行 `tests/` 目录下的端到端及单元测试，部分评估任务依赖 `deepeval`。
- 采用异步 SSE (Server-Sent Events) 模式处理所有长耗时 AI 响应（如聊天流、文章生成），通过 `StreamingResponse` 返回实时进度。
- 实施懒加载单例模式管理重型资源（如 Embedding 模型、Chroma 客户端、Jieba 分词器），在首次调用时初始化以减少启动开销。
- 统一使用 `structlog` 进行结构化日志记录，并在中间件中自动注入 `request_id` 以实现全链路请求追踪。
- 在 RAG 检索链路中采用“本地模型优先，API 降级”的策略，通过 `embed_texts_llm` 自动处理从 BGE 到 DashScope/Zhipu 的故障转移。
- 采用分层结构组织：`architecture/` 定义高层技术栈与数据流，`rag-design.md` 详述 RAG 模块内部组件（Loader, VectorStore, Retriever, QA Chain）及 API 契约。
- `product/` 与 `deployment/` 分别界定业务功能边界与运维环境要求。
- `benchmarks/` 与 `rag-evaluation/reports/` 构成闭环验证体系，通过 `superpowers/plans` 与 `specs/` 驱动迭代，记录了从 v16 到 v20 的检索算法优化路径（如 RRF 融合、Pre-RRF 过滤）。
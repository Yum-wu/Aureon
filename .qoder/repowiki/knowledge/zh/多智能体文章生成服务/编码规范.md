- 采用工厂函数模式（如 `create_researcher`、`create_research_task`）来封装智能体和任务的初始化逻辑，保持主流程代码简洁。
- 在任务描述中使用占位符（如 `{topic}`、`{research_output}`），并在执行前通过字符串替换动态注入上下文数据。
- 所有智能体均配置详细的中文 `role`、`goal` 和 `backstory` 属性，以引导大模型产生符合预期的专业行为。
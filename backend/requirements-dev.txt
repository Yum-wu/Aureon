# Development / Evaluation dependencies (NOT included in production Docker image)
# Install with: pip install -r requirements-dev.txt

# Evaluation frameworks
deepeval>=4.0,<5.0
ragas>=0.1,<0.2

# Test coverage
pytest-cov>=5.0,<6.0
pytest-timeout>=2.3,<3.0

# Type checking
mypy>=1.10,<2.0

# Dependency security audit
pip-audit>=2.7,<3.0

# Linting (also used by pre-commit)
ruff>=0.8.0,<1.0

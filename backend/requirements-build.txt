# Build-time dependencies (NOT included in production image)
# CPU-only PyTorch for embedding model download
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.2.2
accelerate>=0.26.0

# Chatbot Agent Upgrade - Backend Dependencies
# Pin major versions for stability. Requires Python 3.12

langchain>=1.3,<1.4
langchain-openai>=1.2,<1.3
langchain-community>=0.3,<0.4
fastapi>=0.115,<0.116
starlette>=0.40.0,<0.47.0
python-multipart>=0.0.18,<1.0
uvicorn[standard]>=0.34,<0.35
python-dotenv>=1.1.1
tavily-python>=0.5,<0.6
aiofiles>=24.1,<25.0
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<0.25
pytest-xdist>=3.5.0
httpx>=0.28,<0.29
pydantic-settings>=2.0,<3.0

# RAG dependencies
numpy>=1.26,<2.0
langchain-text-splitters>=0.3,<0.4
# Vector store backends (Qdrant is primary; ChromaDB is optional fallback)
# chromadb>=0.5,<0.6  # Optional: pip install chromadb if using VECTOR_BACKEND=chroma
qdrant-client>=1.12.0
# NOTE: torch, sentence-transformers, accelerate are NOT in production requirements.
# They are only used in local dev / GPU environments.
# Railway deploys use API-based embedding (DashScope) and API reranker instead.
# See backend/requirements-build.txt for the Docker build-time model download.
jieba>=0.42

# LangGraph dependencies
langgraph>=1.2,<1.3

# CrewAI (merged into main app)
# ⚠️ crewai 与 langchain>=1.0 不兼容，安装见 crew/requirements.txt
# crewai>=0.11.0,<1.0

# Resilience & rate limiting
tenacity>=8.0,<9.0
slowapi>=0.1.9,<1.0

# Structured logging
structlog>=24.0,<25.0

# Prometheus metrics
prometheus-fastapi-instrumentator>=6.0,<7.0

# Elasticsearch BM25 backend
elasticsearch>=8.10.0

# Redis semantic cache
redis>=5.0,<6.0

# Document format parsers
pypdf>=4.0,<6.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0

# Evaluation frameworks moved to requirements-dev.txt
# deepeval, ragas — install with: pip install -r requirements-dev.txt

# PostgreSQL async (Task 2.1)
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
aiosqlite>=0.20,<1.0

# JWT Authentication (Task 4.2 - RBAC)
PyJWT>=2.8

# Secret encryption (Fernet — encrypt_secret/decrypt_secret)
cryptography>=41.0,<50.0

# OpenTelemetry (Task 5.1 - Distributed Tracing)
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp>=1.20
opentelemetry-instrumentation-fastapi>=0.41b0

# Langfuse tracing (v4 SDK - observation-centric data model)
langfuse>=4.0,<5.0

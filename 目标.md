# Aureon 项目目标与当前状态

> 更新时间：2026-06-14

## 项目概述

Aureon 是一个企业级 RAG（Retrieval-Augmented Generation）知识库系统，基于 Python FastAPI + React 19 构建，支持混合检索、多层记忆、流式响应等特性。

**生产环境**：https://aureon-production-1247.up.railway.app

---

## 核心目标

### 目标 1：高质量 RAG 检索与生成 ✅

| 指标 | 目标值 | 当前值 | 状态 |
|------|--------|--------|------|
| Recall@5 | >=95% | 96.5% | ✅ |
| MRR | >=0.85 | 0.904 | ✅ |
| Answer Relevancy | >=0.75 | 0.943 | ✅ |

### 目标 2：负例检测（拒绝回答超出知识库范围的问题）✅

| 指标 | 目标值 | 当前值 | 状态 |
|------|--------|--------|------|
| Negative Detection | >=80% | 80.0% | ✅ |

**已实现**：
- 关键词快速路径：`_NEGATIVE_KEYWORDS_ZH` 覆盖定价、团队、版本、微服务、QPS 等
- LLM 分类器：关键词未命中时调用 qwen3.5-flash 判断
- 跳过阈值优化：`high_score_skip_threshold=0.1`

### 目标 3：低延迟响应 ✅

| 指标 | 目标值 | 当前值 | 状态 |
|------|--------|--------|------|
| TTFT P50 | <=2000ms | 825ms | ✅ |
| TPOT | <=100ms/tok | 37.0ms/tok | ✅ |
| E2E P50 | <=5000ms | 853ms | ✅ |

**关键优化**：
- 负例查询快速拒绝（853ms vs 之前 12s）
- 流式响应 TTFT 优化

### 目标 4：高忠实度生成（Faithfulness）⚠️

| 指标 | 目标值 | 当前值 | 状态 |
|------|--------|--------|------|
| Faithfulness | >=0.70 | 0.480 | ⚠️ |

**现状分析**：
- qwen3.5-flash 模型倾向于基于自身知识生成内容
- 评估标准可能过严（Judge 给分偏低）
- 需要进一步优化 Prompt 或调整评估标准

**已尝试的优化**：
- ✅ 强化 QA Prompt 约束（中英文同步）
- ✅ 增加评估上下文来源（3→5 sources）
- ⏳ 考虑更严格的 Prompt 约束或模型调优

---

## 技术架构

### 检索 Pipeline
```
Query → Query Router（简单/中等/复杂）→
   ├── 简单 → 纯稀疏向量（<10ms）
   ├── 中等 → Hybrid（dense+sparse）→ 自适应重排序
   └── 复杂 → HyDE → Multi-Query → Hybrid → Ensemble Rerank → 轻量 CRAG
```

### 负例检测流程
```
Query → 关键词快速路径（_NEGATIVE_KEYWORDS_ZH）→
   ├── 命中 → 直接拒绝（~1ms）
   └── 未命中 → LLM 分类器（~2s）→ 可回答/不可回答
```

---

## Benchmark 测试

### 统一测试命令

```bash
# 运行全部 3 阶段（推荐）
cd backend && python tests/run_full_benchmark.py

# 仅 Railway 采集
cd backend && python tests/run_full_benchmark.py --phase 1

# 仅本地 LLM-as-Judge 评估
cd backend && python tests/run_full_benchmark.py --phase 2

# 仅汇总报告
cd backend && python tests/run_full_benchmark.py --phase 3
```

### 测试阶段

1. **Phase 1: Railway 数据采集**
   - 调用 `/api/rag/query` 采集 192 条 QA 数据
   - 流式 TTFT/TPOT 测量（采样 20%）
   - 输出：`benchmark_raw_*.json` + `benchmark_summary_*.json`

2. **Phase 2: 本地 LLM-as-Judge 评估**
   - 使用 qwen3.5-flash 评估 Faithfulness + Answer Relevancy
   - 采样 30 条正例查询
   - 输出：`benchmark_eval_*.json`

3. **Phase 3: 汇总报告**
   - 6 维指标对比（检索质量、生成质量、延迟性能）
   - 输出：`benchmark_report_*.json`

---

## 后续优化方向

### 短期（1-2 周）

1. **提升 Faithfulness 到 0.70+**
   - 优化 QA Prompt 约束
   - 考虑使用更强的模型（如 qwen3-max）
   - 调整评估标准

2. **提升 Answer Completeness 到 90%+**
   - 分析被拒绝的查询类型
   - 优化负例检测边界

### 中期（1 个月）

1. **成本优化**
   - 语义缓存命中率提升
   - LLM 调用频率降低

2. **性能优化**
   - E2E P99 延迟优化
   - 并发处理能力提升

### 长期（3 个月）

1. **企业级特性**
   - 多租户支持
   - SSO/RBAC 集成
   - 审计日志

2. **可观测性**
   - LangFuse 全链路追踪
   - Prometheus 指标监控

---

## 相关文档

- [AGENTS.md](./AGENTS.md) — Agent 开发指令
- [README.md](./README.md) — 项目说明
- [docs/superpowers/plans/](./docs/superpowers/plans/) — 实施计划

---

## 更新日志

- **2026-06-14**：负例检测修复完成（0% → 80%），E2E P50 优化（12s → 853ms）
- **2026-06-13**：6 维 Benchmark 体系升级
- **2026-06-11**：Railway 部署优化
